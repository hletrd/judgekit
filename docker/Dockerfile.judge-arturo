FROM arturolang/arturo:latest
USER root
RUN adduser -D -s /bin/false judge 2>/dev/null || useradd -m -s /bin/false judge && \
    mkdir -p /workspace && chown judge:judge /workspace
WORKDIR /workspace
USER judge
